import { NewProjectForm } from '@/components/projects/NewProjectForm'

export const metadata = { title: 'New project — SceneWeaver' }

export default function NewProjectPage() {
  return <NewProjectForm />
}
